<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="handler" type="de.skicomp.fragments.onboarding.registration.RegistrationContainerFragment" />
    </data>

    <LinearLayout
        android:id="@+id/ll_onboarding_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:orientation="vertical"
        android:focusableInTouchMode="true"
        android:fitsSystemWindows="true"
        android:weightSum="8">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@drawable/background_gradient"
            android:layout_weight="3">

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                app:navigationIcon="@drawable/ic_arrow_bottom"
                app:titleTextAppearance="@style/DefaultText.White.Toolbar"
                app:title="@string/registration_title"/>

            <TextView
                style="@style/DefaultText.White"
                android:id="@+id/tv_registration_toolbar_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_alignParentRight="true"
                android:padding="@dimen/default_padding"
                android:text="1 / 3"
                android:textSize="80sp" />

        </RelativeLayout>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="5">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/onboarding_margin"
                android:orientation="vertical">

                <FrameLayout
                    android:id="@+id/fl_container_registration"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"/>

                <Button
                    style="@style/DefaultButton"
                    android:id="@+id/bt_forward"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="@string/onboarding_register_button_forward"
                    android:onClick="@{handler::onClickForward}"/>

            </LinearLayout>

        </ScrollView>

    </LinearLayout>

</layout>
